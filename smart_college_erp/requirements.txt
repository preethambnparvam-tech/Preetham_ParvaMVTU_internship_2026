# Smart College ERP - Python Dependencies
# Install with: pip install -r requirements.txt

# Core Framework
Django>=4.2,<5.0

# Template helpers
django-widget-tweaks>=1.5.0

# PDF Generation (choose one)
weasyprint>=60.0           # Recommended - full CSS support
# xhtml2pdf>=0.2.11        # Alternative - simpler install

# QR Code Generation
qrcode[pil]>=7.4.2
Pillow>=10.0.0             # Required for image handling

# Excel Export (optional - enhanced)
openpyxl>=3.1.0            # For .xlsx output
# xlwt>=1.3.0              # For .xls output (older format)

# Database (production)
# psycopg2-binary>=2.9.9   # PostgreSQL adapter

# Email
# No extra packages needed - Django has built-in SMTP support

# Development Tools
django-debug-toolbar>=4.0  # Useful during development

# Environment Variables
python-decouple>=3.8       # For managing .env files

# Static files (production)
whitenoise>=6.6.0          # Serve static files efficiently
